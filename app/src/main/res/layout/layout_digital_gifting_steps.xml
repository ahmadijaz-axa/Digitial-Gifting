<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Stepper (simple horizontal LinearLayout for now) -->
    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/stepper"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:src="@drawable/digital_gifiting_step_3"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="@dimen/figma_16_dp"/>

    <!-- "Make It Personal" Title -->
    <androidx.appcompat.widget.AppCompatTextView
        style="@style/Medium1416DarkBlack"
        android:id="@+id/tv_make_it_personal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/make_it_personal"
        android:textSize="@dimen/figma_20_sp"
        android:textColor="@color/grey_universal"
        app:layout_constraintTop_toBottomOf="@id/stepper"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="@dimen/figma_16_dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>